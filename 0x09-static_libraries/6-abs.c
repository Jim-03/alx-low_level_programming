#include "main.h"

/**
 *_abs - outputs absolute value
 *@i: variable to be changed
 *Return: value to be returned
 */

int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}

}
