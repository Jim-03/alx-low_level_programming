#include <stdio.h>

/**
 * main - prints program name
 * @argc: the argument
 * @argv: the array of argc
 * Return: is 0
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
