#include<stdio.h>

/**
 * main - the main function
 * Return: always 0
 */

int main(void)
{
	int i;

	for (i =1 ; i <= 100 ; i++)
	{
		int main(void)
{
	int i;

	for (i =1 ; i <= 100 ; i++)
	{
		if
		{
			if(((i / 3) == 0) && (i / 5) == 0)
	                {
	                        _putchar("FizzBuzz");
				_putchar(' ');        	        }

			else if ((i / 3) == 0)
			{
				_putchar("Fizz");
				_putchar(' ');
			}
			else if ((i / 5) == 0)
			{
				_putchar("Buzz");
				_putchar(' ');
			}
		}
		else
		{
			if (i > 9)
			{
				_putchar(i / 10 + '0');
			}
			_putchar(i % 10 + '0');
		}
		if(i < 100)
		{
			_putchar(' ');
		}
		else
			_putchar('\n');
	}
	return (0);
}
